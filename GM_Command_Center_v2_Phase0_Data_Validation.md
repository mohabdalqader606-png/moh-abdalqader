# GM Command Center v2 — PHASE 0: DATA / FINANCIAL VALIDATION
## (تحقق فقط — بدون أي تعديل HTML/Supabase/SQL)

**المصادر المفحوصة فعلياً (لا افتراضات):** `شاشة_الداشبورد_التنفيذي.html`، `محرك_القوائم_الختامية_والتحليل_المالي_v48.1_FS.html` (فحص مباشر لدالة `calc()`/`calcCore()`، `effLine()`، `S.moves`، `S.accounts`، `S.map`، `S.budget`، `packState()`/`BIGKEYS`).

---

## 1) PHASE 0 VERDICT

**Gross Profit / Gross Margin: نعم، قابلة للحساب بشكل موثوق — لكن على مستوى إجمالي شهري فقط (GL-level)، وليس على مستوى الصنف/العميل.**

`R.IS[k].NETSALES` و`R.IS[k].COGS` بمحرك القوائم الختامية مشتقّتان بالكامل من `S.moves` (قيود الأستاذ العام) مصنَّفة عبر `S.accounts`/`S.map` (شجرة الحسابات وتصنيفها لبنود القوائم) — **وليس** من `S.sales`/`S.purch`/`S.inv` (البيانات التفصيلية). الثلاثة (`moves`, `accounts`, `map`) غير مستثناة من `BIGKEYS`، فهي **تصل فعلياً** لجدول `financials_app_state` بـ Supabase.

**لكن:** القيم المُزامنة هي "المكوّنات الخام" (moves+accounts+map) وليست الأرقام الجاهزة (`R.IS` نفسه غير مُزامَن — يُحسب وقت التشغيل بالمتصفح فقط). يعني: الداشبورد ما بيقدر "يقرأ رقم Gross Profit جاهز" — لازم يُعيد تنفيذ نفس منطق `calc()`/`effLine()` (تجميع قيود حسب الحساب، تصنيفها لبند القائمة، طرح COGS من صافي المبيعات) بكوده الخاص. هاد **بورتّة منطق محدود ومحكم** (حساب GL-aggregation بسيط)، مو إعادة اختراع محرك معقّد — مقبول تقنياً، لكنه **تكرار كود من شاشة تانية** يستحق قرار تصميم واضح منك قبل التنفيذ (انظر بند 6).

---

## 2) Gross Profit Data Architecture

| العنصر | المصدر | الحقول | مستوى البيانات | Limitations |
|---|---|---|---|---|
| NETSALES | `S.moves` عبر `effLine()`→`S.map`/`S.accounts` | حساب/رمز، مدين، دائن، شهر | **إجمالي شهري فقط** (لا صنف، لا عميل) | يعتمد كلياً على دقة تصنيف الحسابات (`S.map`) بمحرك القوائم الختامية — لو التصنيف غلط هناك، الرقم غلط هون تلقائياً |
| COGS | نفس المصدر بالضبط | نفس الحقول | إجمالي شهري فقط | **لا Landed Cost إطلاقاً** بكامل المحرك (تأكدت: صفر إشارة لأي "لاندد كوست" بكل الملف) — يعني COGS محاسبي بحت من القيود، بدون أي تحميل تكاليف شحن/جمارك |
| Gross Profit | `NETSALES - COGS` | — | إجمالي شهري | يرث نفس القيدين أعلاه |
| Gross Margin % | `GP / NETSALES` | — | إجمالي شهري | — |

**الخلاصة الحرجة:** هاد المسار يعطي **مؤشر واحد إجمالي بالـ Level 1** (Gross Profit / Margin % بالشاشة الرئيسية) — لكن **لا يحل** أي من: Margin by Customer، Margin by Item، Margin by Department، أو Profit Bridge's Cost Effect (هاي كلها تحتاج كلفة على مستوى السطر/الصنف، غير موجودة بأي مكان بالنظام — تبقى Gap مفتوحة كما بالتقييم الأصلي، بند منفصل).

**شرط أساسي:** هاد المسار **مشروط** بإنه المستخدم يكون فعلياً حمّل قيود GL (`S.moves`) وعمل تصنيف حسابات (`S.map`/`autoMap`) بمحرك القوائم الختامية، وزامنها. لو ما صار هيك، الحقل يطلع فاضي — والداشبورد لازم يتعامل معه كـ "Data unavailable" مو صفر.

---

## 3) Budget / Target Validation

فحصت `loadBudget()` مباشرة:

- **المستوى:** شهري فقط (`S.budget['YYYY-MM'][lineCode] = amount`) — لا سنوي منفصل (يُجمَّع من الشهري عند الحاجة).
- **لا يوجد بُعد Department إطلاقاً** — الأعمدة الملصوقة أربعة فقط: رمز البند/الحساب، السنة، الشهر، المبلغ. صفر عمود قسم.
- **لا يوجد بُعد Account منفصل عن "البند"** — البند نفسه هو نفس تصنيف `LINES`/`effLine()` (SALES، COGS، ADMIN...)، يعني الموازنة مربوطة ببنود القائمة مباشرة، مو بحسابات GL تفصيلية.
- **هل يمكن ربطه بالمبيعات؟** نعم — لو المستخدم حمّل رقم موازنة لبند `SALES` (أو `NETSALES`) لشهر معيّن، ينقرأ مباشرة كـ Target لذلك الشهر.
- **هل يمكن استخدامه كـ Target للـ Executive Scorecard؟** نعم، لأي بند مُحمَّل (مبيعات، صافي ربح...)، **بشرط إنه محمَّل فعلياً** — مافي ضمان إنه موجود.
- **إذا لم يكن Budget محمَّلاً:** `S.budget = {}` — الشاشة الأصلية نفسها بتعرض "حمّل الموازنة من تبويب البيانات" بدل أي رقم. **نفس السلوك بالضبط لازم يتبناه الداشبورد: Target = "Data unavailable"، مش صفر أو تقدير.**

---

## 4) Profit Bridge Feasibility

| العامل | متوفر؟ | من وين |
|---|---|---|
| **Volume Effect** | ✅ نعم — **اليوم، من بيانات الداشبورد نفسها** | كويري المبيعات الحالي فيه Quantity + صافي المبيعات لكل سطر/صنف/تاريخ — كافي لحساب تباين الكمية بين فترتين (لا يحتاج أي مصدر جديد) |
| **Price Effect** | ✅ نعم — **اليوم، من بيانات الداشبورد نفسها** | نفس الكويري — متوسط السعر (صافي المبيعات÷الكمية) لكل صنف قابل للمقارنة بين فترتين |
| **Cost Effect** | ❌ لا | يحتاج كلفة/COGS على مستوى السطر — COGS المتوفر (بند 2) إجمالي شهري فقط، ما بينفصل لأثر كمية/سعر شراء لكل صنف |
| **Discount Effect** | ❌ لا | كويري المبيعات الحالي يصافي الخصم ضمن الرقم النهائي، بدون عمود خصم منفصل (نفس الفجوة #4 بالتقييم الأصلي) |
| **Returns Effect** | ❌ لا | لا يوجد عمود "نوع المستند" لتمييز المرتجعات (نفس الفجوة #4 بالتقييم الأصلي) |

**الخلاصة:** Profit Bridge بصيغته الكاملة (5 عوامل) **غير مكتمل** — عاملين من خمسة (Volume/Price) ممكنين فوراً بدون أي بيانات جديدة، الباقي يحتاج توسيع كويري المبيعات (نفس التوسيع المطلوب أصلاً بالتقييم الأول، بند #4).

---

## 5) FINAL DATA READINESS MATRIX

| Metric | Available | Source | Reliable | Required Work |
|---|---|---|---|---|
| Net Sales | ✅ نعم | كويري المبيعات الحالي بالداشبورد | نعم — مُختبر فعلياً | لا شي |
| COGS | 🟡 مشروط | `financials_app_state.data` (moves+accounts+map)، **إجمالي شهري فقط** | مشروط بوجود قيود GL + تصنيف حسابات محمَّلين فعلياً بمحرك القوائم الختامية | بورتّة منطق `calc()`/`effLine()` المبسّط + قراءة Read-only للجدول |
| Gross Profit | 🟡 مشروط | نفس مصدر COGS | نفس الشرط | نفس العمل أعلاه |
| Gross Margin | 🟡 مشروط | نفس مصدر COGS | نفس الشرط | نفس العمل أعلاه |
| Budget | 🟡 مشروط | `financials_app_state.data.budget`، شهري بلا قسم | مشروط بالتحميل الفعلي بالشاشة الأخرى | قراءة Read-only فقط |
| Target | 🟡 مشروط | نفس Budget (لا مفهوم منفصل) | نفس الشرط | نفس العمل أعلاه |
| Returns | ❌ لا | — | — | عمود "نوع المستند" بكويري المبيعات |
| Discounts | ❌ لا | — | — | عمود خصم منفصل بكويري المبيعات |
| Inventory Value | ❌ لا | لا يوجد بأي مكان بالنظام | — | Coming Next (كما بالتقييم الأصلي) |
| AP | 🟡 جزئي | كويري المشتريات الحالي (تدفق فواتير فقط، لا رصيد مفتوح) | نعم للتدفق، لا للرصيد | كويري AP Aging جديد (نفس نمط AR الموجود) |
| AR | ✅ نعم | كويري الذمم الحالي | نعم — مُختبر فعلياً | لا شي |

---

## 6) التوصية الدقيقة لـ Phase 1 و Phase 2

**Phase 1 (بدون أي اعتماد على شاشة تانية — مخاطرة منخفضة):**
- توسيع كويري المبيعات: عمود "نوع المستند" (فاتورة/مرتجع) + عمود خصم منفصل + مندوب/قسم (كما بالتقييم الأصلي).
- Volume Effect + Price Effect بـ Profit Bridge (جاهزين من البيانات الحالية بعد إضافة منطق الحساب فقط).
- Returns/Discounts كأرقام منفصلة بمجرد توسيع الكويري.
- كويري AP Aging جديد (نسخة معكوسة من AR الموجود).

**Phase 2 (يعتمد على قرارك بشأن "بورتّة" منطق COGS):**
- إن وافقت: نقل نسخة مبسّطة من `effLine()`/تجميع `moves` لحساب COGS/GP/Margin الإجمالي الشهري + قراءة Budget/Target (Read-only من `financials_app_state`) — **مشروط بوجود بيانات محمَّلة فعلياً هناك**، وإلا "Data unavailable" بلا استثناء.
- Cost Effect بـ Profit Bridge يبقى **Blocked** حتى لو وافقت على بورتّة COGS الإجمالي — لأنه يحتاج كلفة على مستوى الصنف، مو إجمالي شهري (فجوة منفصلة كلياً، غير محلولة بأي من المسارين).

**Margin by Customer / Item / Department:** تبقى **Blocked كلياً** بكل السيناريوهات — لا يوجد أي مصدر كلفة بمستوى الصنف/السطر بأي شاشة بالنظام حالياً. هاي فجوة بيانات SAP حقيقية، مش مسألة تكامل بين الشاشات.

---

## 7) Blockers

1. **COGS/GP مشروط ببيانات فعلية بمحرك القوائم الختامية** — لو ما حد حمّل قيود GL هناك ومصنّفها، القيمة بتضل "Data unavailable" بالداشبورد أيضاً مهما بنينا.
2. **بورتّة منطق `calc()`/`effLine()`** تعني تكرار كود من شاشة تانية داخل الداشبورد — قرار تصميمي يحتاج موافقتك الصريحة (البديل: تركه Blocked ومعروض "Data Required" لحد ما يكون في حل أنظف).
3. **Margin بمستوى الصنف/العميل/القسم و Cost Effect بـ Profit Bridge:** Blocked بشكل كامل ونهائي بالوضع الحالي — يحتاج كويري SAP جديد بعمود كلفة صريح على مستوى السطر (غير موجود بأي كويري بالنظام كله اليوم)، مو مجرد ربط بشاشة تانية.
4. **لا Landed Cost إطلاقاً** بأي مكان بالنظام — أي طلب مستقبلي لـ "Margin after Landed Cost" يبدأ من الصفر بيانياً.
