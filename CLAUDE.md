# هذا المستودع

فيه أكثر من شاشة مستقلة، كل شاشة ملف HTML واحد قائم بحاله (بدون أي اعتماد على ملف آخر):

- `شاشة_عميل_المؤسسة_الاستهلاكية_العسكرية.html` — شاشة عميل المؤسسة الاستهلاكية العسكرية (ربط أصناف، فواتير، سندات تحويل، مطابقات، مزامنة سحابية).
- `شاشة_تحليل_مخالفات_عمر_الذمة_v36.html` — شاشة تحليل مخالفات عمر الذمة.
- `محرك_القوائم_الختامية_والتحليل_المالي_v48.1_FS (جديد).html` — محرك القوائم الختامية والتحليل المالي.
- `index.html` — صفحة هبوط (روابط لباقي الشاشات، بدون منطق خاص فيها).

## قاعدة أساسية: لا تلمس غير الملف المطلوب

كل جلسة/محادثة عادةً بتشتغل على شاشة وحدة بس. **ما تعدّل أو تلمس أي ملف HTML غير الملف يلي طلب المستخدم التعديل عليه بهاي المحادثة**، حتى لو الطلب عام أو يبدو إنه ممكن يفيد شاشة تانية — لأنه ممكن جلسة تانية شغالة بنفس الوقت على ملف مختلف بنفس المستودع، وتعديل ملف غير المقصود ممكن يسبب تضارب أو خلط بين الشاشات.

إذا مش واضح من الطلب أي ملف بالضبط المقصود، اسأل المستخدم قبل ما تعدّل أي شي.

## قبل أي تعديل

اعمل `git fetch origin main` وتأكد إن الفرع المحلي متزامن مع `origin/main` قبل ما تبلش تعديل — ممكن يكون المستخدم عدّل يدوياً (GitHub Desktop) أو جلسة تانية رفعت تعديلات لنفس المستودع بينك وبين آخر مرة اشتغلت.

## بعد أي تعديل على شاشة تعمل مزامنة سحابية (Supabase)

تأكد من `syncheck.js`-style فحص للـ syntax (4 كتل سكربت بكل شاشة تقريباً) قبل الدفع، ولا تدفع مباشرة على `main` إلا بعد اختبار فعلي (Playwright headless) للتغيير.
