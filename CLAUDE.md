# هذا المستودع

مستودع شاشات مركز الكيلاني للأغذية. فيه أكثر من شاشة مستقلة، كل شاشة ملف HTML واحد قائم بحاله (بدون أي اعتماد على ملف آخر)، وكل شاشة إلها جلسة/محادثة خاصة فيها.

## الشاشات (ملفات HTML)

على الفرع `main`:

- `شاشة_عميل_المؤسسة_الاستهلاكية_العسكرية.html` — شاشة عميل المؤسسة الاستهلاكية العسكرية (ربط أصناف، فواتير، سندات تحويل، مطابقات، مزامنة سحابية).
- `شاشة_تحليل_مخالفات_عمر_الذمة_v36.html` — شاشة تحليل مخالفات عمر الذمة.
- `محرك_القوائم_الختامية_والتحليل_المالي_v48.1_FS (جديد).html` — محرك القوائم الختامية والتحليل المالي (تشمل أعمال ضريبة الدخل).
- `index.html` — على `main`: صفحة هبوط (روابط لباقي الشاشات، بدون منطق خاص فيها).

على الفرع `claude/sales-tax-declaration-3j0h7p` (تُنشر على مستودع `kfc-tax` عبر GitHub Pages):

- `شاشة_اقرار_ضريبة_المبيعات_v2.html` — شاشة إقرار ضريبة المبيعات (نسخ متعددة للموظفين، كشف الأمانات، المستوردات الجمركية، تدقيق بيان/فاتورة، Supabase). النسخة الحالية للشاشة.
- `شاشة_اقرار_ضريبة_المبيعات_v1.html` — نسخة قديمة محفوظة للرجوع فقط، لا تُعدَّل.
- `index.html` — على هذا الفرع: **نسخة طبق الأصل** من `شاشة_اقرار_ضريبة_المبيعات_v2.html` (هي ملف النشر الوحيد الذي يرفعه المستخدم للموقع). أي تعديل على الشاشة يُعمل في ملف v2 ثم يُنسخ إلى index.html.

انتبه: معنى `index.html` يختلف حسب الفرع — لا تخلط بينهما أبداً.

## قاعدة أساسية: لا تلمس غير الملف المطلوب

**أي جلسة تشتغل بهذا المستودع ما تعدّل أو تلمس أي ملف غير الملف يلي طلب المستخدم التعديل عليه بالمحادثة الحالية**، حتى لو الطلب عام أو يبدو إنه مفيد لملف/شاشة تانية. السبب: عادةً في أكثر من جلسة شغالة بنفس الوقت، كل وحدة على شاشة مختلفة، وتعديل ملف غير المقصود بيسبب تضارب وخلط بين الشاشات.

- الاستثناء الوحيد: نسخ ملف الشاشة المعدَّلة إلى `index.html` على فرع الإقرار الضريبي (لأنه جزء من آلية نشر تلك الشاشة نفسها).
- إذا مش واضح من الطلب أي ملف بالضبط المقصود، اسأل المستخدم قبل ما تعدّل أي شي.
- لا تعدّل `CLAUDE.md` أو `README.md` أو ملفات SQL إلا بطلب صريح.

## قبل أي تعديل

اعمل `git fetch origin <الفرع>` وتأكد إن الفرع المحلي متزامن مع الريموت قبل ما تبلش — ممكن المستخدم عدّل يدوياً (GitHub Desktop) أو جلسة تانية رفعت تعديلات بينك وبين آخر شغل إلك.

## بعد أي تعديل

- افحص صحة الـ JavaScript قبل الدفع: استخرج كتل `<script>` وشغّل `node --check` عليها.
- ادفع على فرع الجلسة المخصص لها فقط؛ لا تدفع على `main` أو أي فرع آخر بدون طلب صريح من المستخدم.
- الشاشات التي فيها رقم إصدار ظاهر بالفوتر (مثل شاشة الإقرار): ارفع رقم الإصدار مع كل تسليم.
