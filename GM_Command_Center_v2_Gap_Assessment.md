# GM Command Center v2 — BIG 4 Gap Assessment
## (تقييم قبل الترميز — لا يحتوي أي تعديل كود)

**الأساس (Baseline):** `شاشة_الداشبورد_التنفيذي.html` بحالته الحالية (راجع `شاشة_الداشبورد_التنفيذي_HANDOFF.md`).
**منهجية:** كل سطر بالجدول مبني على فحص فعلي للكود (ملف الداشبورد + `المشتريات_الخارجية_الذكية.html` + `شاشة_التدقيق_الداخلي_الإداري.html` + `محرك_القوائم_الختامية_والتحليل_المالي_v48.1_FS.html`) — مش افتراضات. أي نقطة غير مؤكدة 100% موسومة بوضوح "يحتاج تحقق" بدل ما تُفترض.

---

## 0) اكتشافان حاسمان (يحكمان كل خطة التنفيذ)

### 1. محرك المشتريات (`المشتريات_الخارجية_الذكية.html`) — التوصيات **غير محفوظة بأي مكان**
فحصت الملف مباشرة: `STATE._recos` نتيجة حساب **بالذاكرة فقط** (`RECO.allRecommendations()`)، تُعاد حسابها بكل تحميل صفحة، ولا تُكتب لا لـ IndexedDB ولا لـ Supabase. حتى لو الداشبورد فتح نفس قاعدة IndexedDB (`kfc_foreign_procurement_v1`, نفس origin)، ما رح يلاقي جدول توصيات جاهز — بس البيانات الخام (فواتير، مخزون، موردين...). **يعني: ما في طريقة "تقرأ نتائج موجودة فعلاً" بدون إعادة تشغيل نفس محرك التوصيات — وهذا بالضبط اللي طلبت تجنبه ("لا تعيد اختراع المحرك").**

### 2. شاشة التدقيق الداخلي — لا يوجد أي بيانات قابلة للاستعلام
فحصت الملف: الملاحظات/المخاطر/الإجراءات كلها بـ `localStorage` (`kaylani_audit_planner_v1`) على جهاز المستخدم فقط. Supabase مستخدم **فقط** للدخول والصلاحيات (`profiles`, `user_screen_permissions`) — صفر جداول لبيانات التدقيق الفعلية. **لا مزامنة بين الأجهزة أصلاً لهاي الشاشة حالياً.** (ملاحظة: تعليق قديم بـ `index.html` يقول هاي الشاشة "بدون تسجيل دخول" — هذا غير صحيح، الشاشة فعلياً محمية بنفس نظام الصلاحيات؛ التعليق قديم ويحتاج تصحيح لاحقاً كملاحظة جانبية).

**الأثر:** قسمي "Procurement Intelligence" و"Internal Audit Executive View" كما طُلبا حرفياً (قراءة مباشرة لنتائج جاهزة) **غير ممكنين تقنياً اليوم** بدون تعديل على تلك الشاشتين نفسهم (خارج نطاق ملف الداشبورد، ويحتاج إذن صريح منفصل). البدائل الواقعية موضحة بالجدول تحت.

---

## 1) BIG 4 GAP ASSESSMENT

| # | Area | Current State | Gap | Proposed Enhancement | Data Required | Priority |
|---|---|---|---|---|---|---|
| 1 | Sales KPI (Actual/Target/PY/Variance/Trend/Status) | Actual + Prior Year + Trend موجودين وشغالين | لا يوجد Target ولا Status thresholds | قراءة Target من `financials_app_state.data.budget` (Read-only، إن وُجد) + عتبات Status قابلة للتهيئة (مثلاً ±5%/±10%) | موازنة محمَّلة فعلياً بمحرك القوائم الختامية (غير مضمونة) | High |
| 2 | Gross Profit / Margin % | **غير موجود إطلاقاً** — كويري المبيعات الحالي ما فيه كلفة/COGS | فجوة كاملة، وحتى قابلية قراءتها من محرك القوائم الختامية **غير مؤكدة** (انظر Phase 0 تحت) | كويري مبيعات موسّع بعمود كلفة، أو التحقق من إمكانية اشتقاقها من `financials_app_state` | عمود كلفة/COGS بمستوى السطر من SAP — غير موجود بأي كويري حالي بالنظام كله | High (لكن Blocked لحين Phase 0) |
| 3 | Sales by Department / Sales Employee | القسم اختياري (مخفي لعدم بيانات)، المندوب غير موجود إطلاقاً | يحتاج عمود إضافي بكويري المبيعات | إضافة `SlpName`/`OSLP` لكويري المبيعات (نمط موجود فعلاً بمحرك القوائم الختامية — "ربحية العميل") | عمود مندوب/قسم بكويري SAP | Medium |
| 4 | Returns / Discounts (منفصلة) | الكويري الحالي يصافي الخصم ضمن "صافي المبيعات" فقط، بدون فصل | لا يوجد تمييز نوع مستند (فاتورة/مرتجع) ولا قيمة خصم منفصلة | توسيع كويري المبيعات: عمود "نوع المستند" + "قيمة الخصم" منفصلة | أعمدة إضافية بكويري SAP | Medium |
| 5 | Management Attention Center | 4 تنبيهات نصية فعلية (مبيعات/تحصيل/مشتريات مقابل الماضي + ذمم متأخرة كبيرة) | بدون بنية موحدة (Issue/Impact/Owner/Age/Due/Action/Drilldown)؛ معظم أنواع التنبيهات المطلوبة (تجاوز سقف ائتماني، خصم بدون اتفاقية، هامش أقل من حد، مخزون...) **بلا مصدر بيانات حالياً** | إعادة هيكلة التنبيهات الموجودة لبنية سجلات + إضافة الأنواع اللي بياناتها متوفرة فقط | سقف ائتماني (غير موجود)، هامش (يعتمد على #2)، مخزون/صلاحية (غير موجود) | High للبنية، Medium/Blocked لمعظم الأنواع |
| 6 | Enterprise Risk Heatmap | غير موجود | كامل — ومربوط بمشكلة #0.2 (لا بيانات تدقيق قابلة للاستعلام) | **Blocked** لحد ما شاشة التدقيق تتزامن مع Supabase (تعديل منفصل، يحتاج إذن صريح) | جدول Supabase لسجل المخاطر (غير موجود اليوم) | Blocked |
| 7 | Internal Audit Executive View | غير موجود | كامل — نفس مشكلة #0.2 | نفس الحل أعلاه — Blocked تقنياً حتى إشعار آخر | جدول Supabase لملاحظات التدقيق (غير موجود اليوم) | Blocked |
| 8 | Procurement Intelligence ("Today's Decisions") | غير موجود | التوصيات الجاهزة **غير محفوظة بأي مكان** (#0.1) — قراءتها تعني إعادة تشغيل المحرك (ممنوع بطلبك) | Phase 1 عملي: زر/رابط يفتح شاشة المشتريات مباشرة (بدون تكرار بيانات) — تكامل حقيقي لاحقاً فقط لو تلك الشاشة أضافت تصدير/مزامنة (تعديل منفصل بإذن صريح) | نتائج محفوظة فعلياً من محرك المشتريات — غير موجودة اليوم | Blocked لغاية التكامل الحقيقي، Low effort للرابط |
| 9 | Working Capital (AR/Inv/AP Days, CCC) | AR الإجمالي موجود، لا يوجد رصيد مخزون ولا رصيد ذمم دائنة مفتوحة | ينقص: تقييم مخزون + كويري ذمم دائنة (AP aging) مشابه لكويري الذمم المدينة الموجود | إضافة كويري AP aging (نفس نمط كويري AR الحالي، معكوس) | كويري AP aging جديد — بسيط ومشابه للموجود | Medium |
| 10 | 13-Week Cash Forecast | غير موجود إطلاقاً بأي شاشة بالنظام | لا يوجد أي مصدر بيانات رصيد بنكي/جدولة دفعات بكامل المستودع | **لا يُبنى الآن** — يُعرض "Data Readiness / Coming Next" بالضبط كما طلبت | أرصدة بنكية + جدولة دفعات — غير موجودة بأي مكان | Coming Next (لا يُنفَّذ) |
| 11 | Customer 360 / Customer Risk | مبيعات + تحصيل + ذمم لكل عميل موجودين فعلياً (Top Customers) | ينقص: سقف ائتماني، هامش (#2)، مرتجعات/خصومات منفصلة (#4) | صفحة تفصيل عميل من نفس البيانات الموجودة + الحقول الجديدة لما تتوفر | سقف ائتماني من SAP (`OCRD."CreditLine"` عادةً) — غير مُستورد حالياً | Medium |
| 12 | Supplier 360 | قيمة الشراء موجودة (Top Suppliers) | ينقص: اتجاه السعر، مهلة التوريد، أداء التسليم — يحتاج أوامر شراء ومحاضر استلام (غير مستوردة، الكويري الحالي A/P فقط) | كويريز PO/GR جديدة — **نفس الكويريز الجاهزة فعلياً بمحرك المشتريات** (`DATA_SOURCES`) قابلة لإعادة الاستخدام حرفياً | كويري PO + GR (موجود جاهز بشاشة تانية، يُنسخ لا يُخترع) | Medium |
| 13 | Profit Bridge | غير موجود | يعتمد كلياً على #2 (لا كلفة = لا Profit Bridge) | بعد حل #2 فقط | نفس #2 | Blocked (يتبع #2) |
| 14 | Forecast (كل الأنواع) | غير موجود | لا يوجد أي نموذج تنبؤ إحصائي بكامل النظام اليوم | **لا يُبنى الآن** — "Data Readiness / Coming Next" | نموذج/تاريخ بيانات كافٍ — غير مؤكد | Coming Next |
| 15 | Anomaly / Exception Intelligence | جزئياً متداخل مع #5 | معظم الأنواع (قيود يدوية، تسويات مخزون، أنماط إلغاء) بلا مصدر بيانات | فقط "خصم غير اعتيادي" ممكن مبدئياً بعد حل #4 | يعتمد #4 لجزء بسيط، الباقي Coming Next | Low (جزئي) |
| 16 | Management Decisions Log | غير موجود | ميزة جديدة كلياً — سجل قرارات (مو مشتق من SAP) | جدول Supabase جديد (`exec_decisions` مثلاً) + واجهة تسجيل/عرض | **يحتاج جدول SQL جديد — بموافقتك الصريحة فقط (قاعدة CLAUDE.md)** | Medium |
| 17 | Management Pack (زر توليد) | غير موجود | تجميع/تصدير لما هو مبني فعلياً فقط | تصدير طباعة/PDF للأقسام المتوفرة، مع "Data Required" واضحة للأقسام المحجوبة | لا يحتاج بيانات جديدة | Low effort، لكن بعد باقي الأقسام |

---

## 2) TARGET ARCHITECTURE

يبقى نفس النمط الحالي **بدون كسره**:

```
SAP B1/HANA Queries (لصق يدوي، موسّعة بأعمدة إضافية عند الحاجة)
        ↓
Local Calculation (نفس الفهارس IDX.byDay/byMonth + دوال حساب جديدة)
        ↓
Result JSON → Supabase (exec_dashboard_state) — كما هو
        ↓
+ قراءة إضافية (Read-Only) اختيارية من financials_app_state
  لقيمة الموازنة (Target) فقط — لا كتابة، لا تعديل على تلك الشاشة
```

**لا نقل لباك-إند جديد، لا تغيير على SAP queries الموجودة، لا تعديل على schema شاشات تانية.** أي تكامل حقيقي مع المشتريات/التدقيق يتطلب تعديل صغير على تلك الشاشات نفسها (مزامنة ملخص لـ Supabase) — وهذا **خارج نطاق هالجلسة** إلا بطلب صريح منفصل لكل شاشة (لأنه يكسر قاعدة "ملف واحد بالمحادثة").

---

## 3) PAGE INFORMATION ARCHITECTURE

التصميم الحالي (تمرير واحد طويل) ما بيتحمّل كل هاي المستويات — لازم ينتقل لتنقّل بعلامات تبويب (Tabs) أعلى الصفحة، فوق الـ Level 1 الثابت:

```
[ Level 1 — Executive Snapshot ]  ← ثابت دائماً أعلى الصفحة، كل التابات
──────────────────────────────────────────
Tabs: [أداء المبيعات] [الربحية] [المتابعة الإدارية] [المخاطر والحوكمة]
      [القرارات] [العملاء] [الموردين] [رأس المال العامل] [تقرير الإدارة]
```

كل تاب = قسم من الأقسام أعلاه، بما فيها الأقسام المحجوبة تظهر بحالة "Data Required" واضحة بدل ما تختفي (شفافية بدل إخفاء).

---

## 4) DATA READINESS MATRIX (ملخص)

| الحالة | الأقسام |
|---|---|
| ✅ متوفر فعلياً (Baseline) | Sales Actual/PY/Trend، Collections، Purchases (A/P فقط)، AR + Aging، Top Customers/Products/Suppliers |
| 🟡 يحتاج توسيع كويري بسيط (نفس النمط الحالي) | مندوب/قسم، نوع مستند+خصم منفصل، AP Aging، PO+GR (منسوخة من محرك المشتريات) |
| 🟠 يحتاج تحقق تقني (Phase 0) | Gross Profit/Margin عبر `financials_app_state` |
| 🔴 Blocked — يحتاج تعديل شاشة تانية بإذن منفصل | Procurement Intelligence الحقيقي، Internal Audit Executive View، Risk Heatmap |
| 🔴 Blocked — يحتاج جدول SQL جديد بموافقتك | Management Decisions Log |
| ⚪ Coming Next — لا يوجد أساس بيانات بالنظام كله | 13-Week Cash Forecast، Forecast models، معظم Anomaly Intelligence |

---

## 5) IMPLEMENTATION PHASES (مقترحة)

- **Phase 0 — تحقق (قبل أي كود):** فحص دقيق هل `financials_app_state` (الموازنة + بيانات GL) تكفي لاشتقاق Gross Profit/Target بدون إعادة بناء منطق `calcCore` الخاص بمحرك القوائم الختامية، أو إنه يحتاج كويري كلفة منفصل. قرار هالنقطة بيحدد شكل Phase 2.
- **Phase 1:** توسيع كويري المبيعات (مندوب/قسم/نوع مستند/خصم) + كويري AP Aging جديد + إعادة هيكلة Management Attention Center لبنية سجلات + Customer 360 (من بيانات موجودة أصلاً) + رابط مباشر لشاشة المشتريات (بدون تكرار بيانات).
- **Phase 2:** Gross Profit/Margin + Profit Bridge (حسب نتيجة Phase 0) + Target من الموازنة (قراءة فقط) + Working Capital (AR/Inventory/AP Days، CCC) + Supplier 360 (كويريز PO/GR منسوخة من محرك المشتريات).
- **Phase 3 (بموافقتك الصريحة، يشمل SQL جديد):** Management Decisions Log + Management Pack Export.
- **Phase 4 (يحتاج إذن منفصل لتعديل شاشات تانية):** مزامنة حقيقية لملخص التدقيق الداخلي + المشتريات لـ Supabase → عندها فقط Risk Heatmap وInternal Audit Executive View وProcurement Intelligence الحقيقي تصير ممكنة.
- **لا تُبنى إطلاقاً حالياً:** 13-Week Cash Forecast، Forecast الإحصائي — حسب قاعدتك "لا تخترع" بالضبط.

---

## 6) RISKS / BREAKING CHANGES

- **تغيير بنية الصفحة لتابات** يغيّر تجربة v1 (تمرير واحد) — لازم يحافظ على كل KPIs/الأقسام الحالية بلا استثناء أثناء إعادة الهيكلة (خطر Regression حقيقي، يحتاج اختبار شامل بعد كل مرحلة).
- **توسيع كويري المبيعات بأعمدة جديدة** يعني نتائج قديمة ملصوقة (بدون الأعمدة الجديدة) لازم تبقى تشتغل بدون كسر — الـ parser الحالي متساهل بالفعل مع أعمدة اختيارية، هالمبدأ لازم يستمر.
- **قراءة `financials_app_state` من شاشة تانية**: قراءة فقط (Read-only)، خطر منخفض، لكن لازم تتعامل بأمان مع حالة "الجدول/الصف غير موجود أو فاضي" (نفس نمط try/catch المستخدم حالياً).
- **جدول SQL جديد لسجل القرارات**: يحتاج موافقتك الصريحة وتنفيذك اليدوي لـ SQL (نفس ما صار بجدول `exec_dashboard_state`).
- **لمس شاشتي المشتريات/التدقيق مباشرة**: غير مسموح بهالجلسة بدون طلب صريح منفصل لكل شاشة — أي تكامل حقيقي فيهم مرحلة منفصلة كاملة.
- **حجم الطلب ككل** أقرب لمنصة GRC/BI متكاملة من "شاشة واحدة" — التوصية: تنفيذ مرحلي صارم مع مراجعة/موافقة بعد كل مرحلة، بدل بناء كل شي دفعة وحدة.
