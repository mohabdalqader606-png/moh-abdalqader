# الداشبورد التنفيذي — Executive Dashboard
## ملخص تسليم (HANDOFF)

**الحالة:** 🟡 قيد الاختبار — الشاشة مبنية وكاملة الوظائف، وبنختبر حالياً كويريات SAP وحدة وحدة قبل الاستخدام الفعلي
**الملف:** `شاشة_الداشبورد_التنفيذي.html` (ملف مستقل واحد، ~1,250 سطر، بدون بيانات مضمّنة)
**الفرع:** `claude/laughing-sagan-pxi0jb`
**آخر تحديث:** 2026-09-22

هاد الملف مرجع لأي حد (إنسان أو AI) بياخذ الشغل من بعدنا على هاي الشاشة — شو تم بناؤه، شو القرارات اللي اتخذناها وليش، وشو المتبقي.

---

## أ) الفكرة والبنية

شاشة تنفيذية واحدة (Single-page) للمدير العام، بدون أي اعتماد على ملف خارجي — نفس نمط باقي شاشات المستودع:

```
┌──────────────────────────────────────────────────────────┐
│  UI LAYER: فلاتر الفترة + KPI Cards + Sales Chart (SVG    │
│  يدوي، بدون مكتبة) + Top Customers/Products/Suppliers +   │
│  Collections/Purchases/AR + Executive Alerts               │
├──────────────────────────────────────────────────────────┤
│  ENGINE LAYER: computeKPIs() — يحسب من RAW بالمتصفح فقط    │
│  (لا SQL على السيرفر، لا Backend) — فهارس Map مجمّعة مسبقاً  │
│  (IDX.byDay / IDX.byMonth) لتفادي إعادة الحساب الثقيلة      │
├──────────────────────────────────────────────────────────┤
│  DATA LAYER: لوحة "استيراد بيانات SAP" — لصق نتيجة 6        │
│  كويريز HANA يدوياً، تُحسب بالمتصفح، والنتيجة المحسوبة فقط    │
│  (مو البيانات الخام) تُزامَن لجدول Supabase exec_dashboard_state │
└──────────────────────────────────────────────────────────┘
```

**قرار معماري مهم:** فحصنا النظام قبل البناء ولقينا إنه ما في جدول Supabase حي فيه بيانات مبيعات/ذمم/مشتريات حقيقية — كل الشاشات الموجودة (مبيعات المؤسسة العسكرية، عمر الذمة، محرك القوائم الختامية...) تشتغل بنفس الطريقة: لصق نتيجة كويري SAP → حساب بالمتصفح → مزامنة النتيجة المحسوبة فقط. الداشبورد التنفيذي اتبع نفس النمط بدل ما يخترع بنية جديدة، بلوحة استيراد خاصة فيه.

---

## ب) مصادر البيانات (6 كويريز SAP HANA)

معرّفة بـ `IMPORT_SOURCES` بالملف، كل وحدة فيها: عنوان، نص SQL جاهز للنسخ، ملاحظة، ودالة `parse()` تحوّل النص الملصوق لبيانات. المطابقة على الأعمدة مرنة (اسم مطابق أو يحتويه)، فمش حساسة لو رتبة الأعمدة اختلفت.

| # | المصدر | الجداول | الحالة |
|---|---|---|---|
| 1 | فواتير المبيعات (A/R) — Actual SAP Sales | `OINV`/`INV1` | 🟡 قيد الاختبار — صُححت (انظر "أخطاء انصلحت") |
| 2 | التحصيل | `ORCT` | ⏳ لم تُختبر بعد |
| 3 | فواتير المشتريات (A/P) | `OPCH`/`PCH1` | ⏳ لم تُختبر بعد |
| 4 | الذمم المدينة المفتوحة (AR Aging) | `OINV` | ⏳ لم تُختبر بعد |
| 5 | رصيد مستودع 011 آخر كل شهر (تاريخياً) | `OINM` | ⏳ لم تُختبر بعد — صُححت للتوافق مع HANA (انظر تحت) قبل التجربة |
| 6 | قائمة أسعار المؤسسة العسكرية | `ITM1`/`OITM` | ⏳ لم تُختبر بعد — **يحتاج تعبئة رقم قائمة السعر الصحيح** (`PriceList = <رقم>`) قبل التشغيل — الرقم غير معروف حالياً، غير موجود بأي مكان بالنظام الحالي |

كل كويري تظهر بلوحة "استيراد بيانات SAP" (زر بأعلى الشاشة)، مع textarea للصق النتيجة (Tab-separated مع سطر العناوين).

---

## ج) أخطاء SAP انصلحت أثناء الاختبار (مهم لأي جلسة قادمة)

النظام شغال على **SAP HANA** (`LIBODBCHDB`) مو SQL Server — عدّة فروقات صيغة انصلحت:

1. **`ISNULL` → `IFNULL`** — كويري المبيعات كانت فيها `ISNULL(T0."DiscPrcnt",0)`، طلعت خطأ `inconsistent datatype`. الصيغة الصحيحة بـ HANA (ومستخدمة فعلياً بباقي شاشات المستودع) هي `IFNULL`.
2. **تواريخ ثابتة → معاملات SAP B1**: بدل ما نكتب `WHERE T0."DocDate" >= '[من]' AND <= '[إلى]'` (نص placeholder كان بيتكتب حرفياً لو ما انتبه المستخدم)، استُبدلت بـ `BETWEEN [%0] AND [%1]` — صيغة SAP B1 Query Manager القياسية (نفس المستخدمة بشاشة "مبيعات المؤسسة العسكرية حسب الفرع") اللي بتفتح نافذة اختيار تاريخ تلقائية عند التشغيل.
3. **كويري رصيد مستودع 011**: كانت مكتوبة بصيغة T-SQL (`;WITH`, `CONVERT(varchar(7),...,120)`, `ROWS UNBOUNDED PRECEDING`) — صُححت لصيغة HANA (`WITH` بدون فاصلة منقوطة، `TO_VARCHAR(...,'YYYY-MM')`، `ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW`) **قبل** ما توصل هاي الكويري للاختبار الفعلي، بس لسا ما جُربت حياً — احتمال في فروقات إضافية (خصوصاً أسماء أعمدة `OINM`: `CreateDate`/`TransId`/`InQty`/`OutQty` — لازم تتأكد إنها مطابقة لمخطط SAP عندكم).

**إذا كويري 2/3/4/6 طلعت خطأ SQL مشابه، غالباً نفس السبب (HANA vs SQL Server) — راجع الصيغة قبل أي تعديل تاني بالمنطق.**

---

## د) قاعدة مبيعات المؤسسة الاستهلاكية العسكرية (قرار مهم وموثّق)

بطلب صريح من المستخدم، الصيغة المعتمدة هي **الحرفية** من السؤال الأصلي:

```
Military Sales (لكل شهر) = رصيد مستودع 011 آخر ذلك الشهر × سعر الصنف من قائمة الأسعار العسكرية
```

هاي صيغة **جديدة كلياً** — مختلفة عن منطق شاشة "مبيعات المؤسسة العسكرية حسب الفرع" الموجودة (اللي بتحسب من صافي كمية التحويل × متوسط سعر فرع عمّان الفعلي، مع حماية موثّقة من التكرار بين فرع العقبة والباقي).

**منع الازدواجية:** بما إنه ما في آلية مؤكدة تربط هاي الصيغة الجديدة بفواتير SAP الفعلية (لا تاريخ، لا رقم فاتورة مشترك)، اتّبعنا تعليمة المستخدم بالنص الأصلي: **المؤشر معزول تماماً**، ما بينضاف لإجمالي المبيعات الرسمي (Actual SAP Sales)، وبيظهر بقسم "تركيبة المبيعات" مع تنويه صريح بالسبب. ما في "Total Executive Sales" مدموج بالشاشة لهالسبب بالذات.

**تاريخياً:** كل شهر بياخذ رصيده الخاص من كويري رقم 5 (مو الرصيد الحالي) — عشان مقارنة 2026 مع 2025 تكون دقيقة تاريخياً كما طلب المستخدم بالنص الأصلي (بند 19).

---

## هـ) البنية التحتية المُنشأة (Supabase) — تمت ✅

1. **جدول `exec_dashboard_state`** — بلوب JSON واحد (صف `id='main'`) يخزّن نتيجة آخر استيراد محسوب، لمزامنة الشاشة بين الأجهزة/المستخدمين. ✅ أُنشئ من طرف المستخدم.
2. **صف `screens`** لـ `id='exec-dashboard'` + صلاحية ADMIN للمستخدم عبر شاشة إدارة المستخدمين والصلاحيات. ✅ تم.
3. **بطاقة بالبوابة (`index.html`)** — أُضيفت بفئة "المالية والتحليل"، وصُحح خلل كانت البطاقة بسببه مخفية دائماً (نسيان `PORTAL_APP_GATE["APP-FIN-04"]`). ✅ تم الدمج بـ `main`.

---

## و) المتبقي فعلياً

- [ ] **اختبار الكويريز 2-6** وحدة وحدة (نفس أسلوب الكويري 1) والتأكد من تطابق الأعمدة.
- [ ] **رقم قائمة الأسعار العسكرية** بكويري 6 — لازم يُعبّى من `OPLN` عند المستخدم (غير معروف حالياً).
- [ ] **التحقق من أعمدة `OINM`** بكويري 5 (`CreateDate`, `TransId`, `InQty`, `OutQty`, `Warehouse`) تطابق مخطط SAP الفعلي.
- [ ] تجربة الشاشة فعلياً ببيانات حقيقية كاملة (KPIs، الرسم البياني والتنقيب اليومي، القوائم، التنبيهات).

---

## ز) اقتراحات إضافية (لم تُنفَّذ حفاظاً على البساطة — حسب تعليمة المستخدم بعدم توسيع النطاق)

- فلتر "القسم/الفرع" جاهز بالكود (`#deptFilterGrp`) لكنه مخفي — يحتاج فقط عمود قسم إضافي بكويري المبيعات ليظهر تلقائياً.
- تفصيل Purchase Orders / Goods Receipts منفصلة عن A/P Invoices (حالياً مؤشر واحد فقط للمشتريات).
- قيمة المخزون (Inventory KPI) — لا يوجد مصدر بيانات لها حالياً، تظهر "Data unavailable" بشكل دائم.

---

## ح) ملاحظات تقنية للمطوّر القادم

- التصميم: نفس design tokens بمعظم شاشات المستودع (`--navy`, `--k-*`, خطوط Noto Kufi Arabic / IBM Plex Sans Arabic / IBM Plex Mono)، مع طبقة "Executive" داكنة إضافية بالهيدر (`--hdr-*`).
- الرسوم البيانية: SVG يدوي بالكامل (بدون مكتبة خارجية) — دالة `lineChartSVG()` قابلة لإعادة الاستخدام لأي رسم خطي شهري/يومي مستقبلاً.
- **خلل bidi انصلح:** أي مكان بيعرض رقم + "د.أ" لازم يستخدم `fmtCurHTML()` (يفصل الرقم بـ `<span class="mono">` معزول الاتجاه عن وحدة العملة) — استخدام `fmtCur()` النصي العادي جوا عنصر بخط mono مباشرة بيقلب ترتيب حروف "د.أ" بصرياً (مُثبت بالاختبار). `fmtCur()` النصي آمن فقط بسياق بدون خط mono (مثل نصوص التنبيهات).
- الفحص: `node --check` على كتلة `<script>` المستخرجة قبل كل دفع (per CLAUDE.md) — تم بكل تسليم.
