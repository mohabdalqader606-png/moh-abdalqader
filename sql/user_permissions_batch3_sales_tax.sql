-- ============================================================================
-- USER & PERMISSIONS — BATCH 3: تسجيل "إقرار ضريبة المبيعات" + إنفاذ فعلي
-- ============================================================================
-- هذا الملف SQL جاهز للتشغيل اليدوي على Supabase SQL Editor فقط.
-- لم يُشغَّل من قبل Claude — لا يوجد اتصال مباشر بقاعدة البيانات بهذه الجلسة.
-- السطر الوحيد هنا: إضافة صف جديد بجدول screens الموجود مسبقاً (Batch 1).
-- لا تعديل على أي جدول آخر، لا تعديل على أي صف موجود.
-- ============================================================================

insert into public.screens (id, name, file, status, screen_type) values
  ('sales-tax-declaration', 'إقرار ضريبة المبيعات', 'شاشة_اقرار_ضريبة_المبيعات_v2.html', 'active', 'business')
on conflict (id) do nothing;

-- ملاحظة: بما إن مصفوفة الصلاحيات تستعلم screens حيث screen_type='business'،
-- هذا الصف سيظهر تلقائياً كعمود ثامن بشاشة إدارة المستخدمين والصلاحيات
-- بدون أي تعديل إضافي على تلك الشاشة.

-- ============================================================================
-- ⚠️ خطوة إلزامية قبل تفعيل كود الإنفاذ على شاشة الإقرار (Batch 3 — Development):
-- ============================================================================
-- شاشة إقرار ضريبة المبيعات مستخدَمة فعلياً اليوم من عدة موظفين بدون أي بوابة دخول
-- (allowed_apps) — أي مستخدم صالح بـSupabase يدخلها حالياً. بعد رفع كود الإنفاذ،
-- "NONE" (غياب الصف) = منع الدخول كلياً. لذلك يجب:
--
--   1) الدخول على شاشة إدارة المستخدمين والصلاحيات بعد تشغيل الإدراج أعلاه.
--   2) تعيين صلاحية صريحة (VIEW / INPUT / ADMIN) لكل مستخدم يستخدم شاشة الإقرار
--      فعلياً اليوم، على عمود "إقرار ضريبة المبيعات" تحديداً.
--   3) تشغيل الاستعلام التالي للتأكد إنه ما في أي مستخدم فعّال بقي بدون صف
--      (استبدل القائمة بالإيميلات الفعلية لمستخدمي شاشة الإقرار):
--
-- select p.email, p.full_name, p.role
-- from public.profiles p
-- where p.email in ( /* ضع هنا إيميلات كل موظفي شاشة الإقرار الحاليين */ )
--   and not exists (
--     select 1 from public.user_screen_permissions u
--     where u.user_id = p.id and u.screen_id = 'sales-tax-declaration'
--   );
--
--   -- النتيجة يجب أن تكون فارغة (0 صفوف) قبل الموافقة على دفع كود الإنفاذ.
--
-- لا يُدفَع كود الإنفاذ لفرع شاشة الإقرار قبل تأكيدك أن هذا الاستعلام يرجع صفراً.
-- ============================================================================
