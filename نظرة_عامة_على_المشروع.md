# مركز الكيلاني للأغذية — نظرة عامة على المشروع

**آخر تحديث:** 2026-09-22
**المستودع:** `mohabdalqader606-png/moh-abdalqader`

هاد ملخص عام لبنية المستودع بالكامل — مرجع سريع لأي جلسة (إنسان أو AI) بدها تفهم شكل النظام قبل ما تشتغل على أي شاشة.

---

## أ) الفكرة العامة

مجموعة شاشات إدارية/تحليلية مستقلة لمركز الكيلاني للأغذية، كل شاشة **ملف HTML واحد قائم بحاله** — بدون أي اعتماد على ملف خارجي (لا CSS مشترك، لا JS مشترك). كل شاشة بمحادثة/جلسة خاصة فيها، وعادةً بأكثر من جلسة شغالة بنفس الوقت على شاشات مختلفة.

**بوابة الدخول:** `index.html` (على فرع `main`) — صفحة هبوط واحدة بتعرض بطاقات لكل الشاشات المسجَّلة، مفلترة حسب صلاحية المستخدم المسجّل دخوله.

---

## ب) النمط المعماري المشترك (كل الشاشات تقريباً)

```
┌────────────────────────────────────────────────────────┐
│  UI: HTML+CSS+JS بملف واحد، RTL عربي، design tokens      │
│  متكررة (--navy/--k-*/--kd-*)، خطوط Noto Kufi Arabic /   │
│  IBM Plex Sans Arabic / IBM Plex Mono                    │
├────────────────────────────────────────────────────────┤
│  AUTH: Supabase Auth (بريد/كلمة مرور) — نفس مشروع        │
│  Supabase بكل الشاشات (fxjdmqqmcgccocfbgopm)              │
│  بوابتان: profiles.allowed_apps (قديمة) +                │
│  user_screen_permissions (VIEW/INPUT/ADMIN — الحالية)     │
├────────────────────────────────────────────────────────┤
│  DATA: لا جداول SQL حية فيها بيانات SAP حقيقية. كل شاشة   │
│  فيها كويريز SAP B1/HANA جاهزة (يلصقها المستخدم يدوياً)،   │
│  تُحسب بالكامل بالمتصفح (JS)، والنتيجة المحسوبة فقط        │
│  (JSON blob) تُزامَن لجدول Supabase خاص بكل شاشة           │
│  (مثلاً mil_branch_sales_state، debtaging_app_state)       │
└────────────────────────────────────────────────────────┘
```

**مهم:** أي شاشة جديدة أو تعديل على البيانات لازم يتبع نفس النمط (لصق → حساب محلي → مزامنة نتيجة) إلا لو في سبب واضح لخلاف هيك.

---

## ج) جرد الشاشات

### مسجَّلة بالبوابة (`index.html` على `main`)

| الشاشة | الملف | الفئة | ملاحظة |
|---|---|---|---|
| عميل المؤسسة الاستهلاكية العسكرية | `شاشة_عميل_المؤسسة_الاستهلاكية_العسكرية.html` | المبيعات | ربط أصناف، فواتير، سندات تحويل، مطابقات |
| مبيعات المؤسسة العسكرية حسب الفرع | `شاشة_مبيعات_المؤسسة_العسكرية_حسب_الفرع.html` | المبيعات | مبيعات كل فرع + مطابقة سندات التحويل مع الفواتير |
| تحليل مخالفات عمر الذمة | `شاشة_تحليل_مخالفات_عمر_الذمة_v36.html` | المالية | أعمار الذمم + التحصيل اليومي |
| محرك القوائم الختامية والتحليل المالي | `محرك_القوائم_الختامية_والتحليل_المالي_v48.1_FS (جديد).html` | المالية | قوائم ختامية، تحليل مالي، ضريبة دخل |
| إقرار ضريبة المبيعات | (مشروع خارجي منفصل) | المالية | `mohabdalqader606-png/kfc-tax` — فرع `claude/sales-tax-declaration-3j0h7p` بهاد المستودع |
| **الداشبورد التنفيذي** ⭐ جديد هالجلسة | `شاشة_الداشبورد_التنفيذي.html` | المالية | لوحة قيادة تنفيذية للمدير العام — انظر `شاشة_الداشبورد_التنفيذي_HANDOFF.md` |
| المشتريات والمخزون 2026 | `المشتريات_الخارجية_الذكية.html` | المشتريات | توصيات شراء SAP-aligned — انظر `المشتريات_الخارجية_الذكية_HANDOFF.md` |
| نظام إدارة التدقيق الداخلي والحوكمة | `شاشة_التدقيق_الداخلي_الإداري.html` | التدقيق | GRC، ملاحظات، مخاطر، خطة سنوية |
| أسطول السيارات | `شاشة_أسطول_السيارات.html` | الأسطول | ملخص يومي لكل سائق |
| إدارة المستخدمين والصلاحيات | `شاشة_إدارة_المستخدمين_والصلاحيات.html` | النظام | مصفوفة الصلاحيات (`screens` + `user_screen_permissions`) |
| كشوفات التوالف | `شاشة_كشوفات_التوالف.html` | المخزون | كشوفات توالف وإتلاف — مستودع 100 |

### غير مسجَّلة بالبوابة (ملفات قديمة/يتيمة بالمستودع)

| الملف | الحالة |
|---|---|
| `شاشة_ذكاء_المشتريات_والمخزون.html` | النسخة **القديمة** — استُبدلت بـ "المشتريات والمخزون 2026"، لم تُحذف من المستودع |
| `قوائم_الأعمال.html` | ملف قديم غير مرتبط بالبوابة حالياً |

⚠️ **ملاحظة أمان موثَّقة بـ `index.html`:** شاشتا "التدقيق الداخلي" و"قوائم الأعمال" ما عندهم نظام دخول خاص فيهم لسا — فتح رابطهم مباشرة بيدخل بدون قيد حتى لو مخفيين عن البوابة.

---

## د) الفروع (Branches)

- **`main`** — كل الشاشات أعلاه + `index.html` (البوابة الحقيقية).
- **`claude/sales-tax-declaration-3j0h7p`** — شاشة إقرار ضريبة المبيعات (v1 وv2) + `index.html` **مختلف المعنى هون**: نسخة طبق الأصل من v2، هو ملف النشر لموقع GitHub Pages لمشروع `kfc-tax` المنفصل.
- **فروع مؤقتة لكل جلسة/مهمة** (مثل `claude/laughing-sagan-pxi0jb` — فرع هالجلسة) — تُدمج بـ `main` يدوياً بعد التأكد من الشغل.

---

## هـ) الصلاحيات (Permissions)

جدولان بـ Supabase:
- `screens(id, name, file, status, screen_type)` — سجل كل شاشة.
- `user_screen_permissions(user_id, screen_id, permission[VIEW|INPUT|ADMIN])` — صلاحية كل مستخدم على كل شاشة.

**ملف SQL جديد لازم يُنشأ (batch) لكل شاشة جديدة** بمجلد `sql/` — لا تُنشأ تلقائياً، ولا تُلمس ملفات SQL إلا بطلب صريح (قاعدة CLAUDE.md).

⚠️ **فخ شائع (وقعنا فيه هالجلسة):** إضافة شاشة لمصفوفة `APPS` بـ `index.html` وحدها ما كافية — لازم كمان تُضاف لجدول `PORTAL_APP_GATE` (بنفس الملف) اللي بيربط `app.id` بـ `screen_id`، وإلا البطاقة تختفي تلقائياً من البوابة لأي مستخدم.

---

## و) قواعد العمل الأساسية (من `CLAUDE.md`)

1. **ملف واحد بالمحادثة** — ما تلمس غير الملف المطلوب، حتى لو الطلب عام. اسأل لو مش واضح.
2. **مزامنة قبل التعديل** — `git fetch` والتأكد من مطابقة الفرع المحلي للريموت قبل أي شغل.
3. **فحص JS قبل الدفع** — استخراج كتل `<script>` وتشغيل `node --check` عليها.
4. **الدفع على فرع الجلسة فقط** — لا دفع على `main` أو فرع تاني بدون طلب صريح.
5. **لا تعديل CLAUDE.md/README.md/ملفات SQL** إلا بطلب صريح.

---

## ز) هالجلسة (claude/laughing-sagan-pxi0jb)

بُنيت شاشة **الداشبورد التنفيذي** من الصفر (KPIs، اتجاه مبيعات شهري مع تنقيب يومي، مقارنة تلقائية بالسنة الماضية، أهم عملاء/أصناف/موردين، تحصيل، مشتريات، ذمم مدينة مع aging، تنبيهات تنفيذية) + ربطها بالبوابة + إصلاح `PORTAL_APP_GATE`. التفاصيل الكاملة والقرارات المعمارية بملف `شاشة_الداشبورد_التنفيذي_HANDOFF.md`. حالياً قيد اختبار كويريات SAP الستة وحدة وحدة مع المستخدم (توافق HANA).
